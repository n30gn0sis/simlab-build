# Cut 2026-09-26 (UTC) — `bundle-20260926` on staging VM 9771: first clean end-to-end `--strict` PASS

> **Transfer candidate.** Built on VM 9771 (this session's own staging VM), which nothing else touched during the run: the VM's sudo journal holds only this session's 5 commands. It was **not transferred**; the bundle sits on 9771's disk (VM stopped, not rolled back).

Raw logs are in `staging-cut-2026-09-26/`: `preflight.log`, `cut-tail.log`, `BUNDLE_NOTES.md.txt`, `summary.txt`.

| | |
|---|---|
| VM | 9771 `r770-staging-2`, first boot from cold snapshot `clean-2026-09-25`. DHCP gave **192.168.4.23** (found by MAC `BC:24:11:E7:AF:99`); new host key `SHA256:0+EgAnfmCYLGPeosPw9pGl9VBucnOhHK2jixaNuFOZA`, generated by cloud-init on the clone's first boot |
| Source | `claude/opnsense-mirror` @ `03d464b` = `main` @ f1e098e plus the OPNsense mirror fix (#12) |
| Preflight | exit 0 |
| Builder | `r770-build-bundle.sh --yes --bundle-dir …/bundle-20260926` → **exit 0** |
| Bundle | 15G, 1598 manifest entries, 855 debs (size/count owner: `bundles.md`) |
| `BUNDLE_NOTES.md` | **no WARN lines** |
| APT | `Packages` + `Packages.gz` + **`Release`** (lists both, not itself) — the #8 fix |
| Appliances | OPNsense 26.7 dvd + sha256 + sig (from `pkg.opnsense.org`), CHR 7.24.4, VyOS rolling + minisig, Alpine + sha256 — 8 files |
| `verify --strict` | **RESULT: PASS — bundle is complete, unmodified and ready to transfer** |

## Why it took three attempts on 2026-09-25

1. `mirrors.dotsrc.org` stopped answering TCP (two days running) → the three OPNsense files WARNed. Fixed by moving to `pkg.opnsense.org` (#12).
2. On VM 9770 a second session (`sim-lab-basic-08`) ran `--only appliances`, a strongSwan node and `--only manifest`/`verify` against the same bundle during this session's builder run. That caused the colliding CHR download, the duplicated notes, and the notes/manifest checksum mismatch. Fixed by giving this session its own VM, 9771 (#13). The CHR 7.24.4 download itself works; it was reproduced cleanly from the session.

## Not yet done

The ISO GPG signature is re-verified by every fetch (see the notes). The remaining steps are the builder's own "Next": copy to ext4 media, gate again from the media, then install runbook on the R770.
